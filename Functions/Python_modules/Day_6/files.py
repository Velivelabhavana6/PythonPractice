import os
os.getcwd
print(os.listdir('C:\\Users'))
#shutil module is to move one loc to anoyher loc
# send2trash is to remove the file in listdir(Back to curr loc)
#os.walk() is used to look like a tree everything it should represent like fol,subfolo,file ...etc